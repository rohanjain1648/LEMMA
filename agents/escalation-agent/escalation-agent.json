{
  "name": "escalation-agent",
  "description": "Reads the triaged + drafted ticket and the customer tier, then decides whether to auto-send or route to human review. Returns a structured decision; a deterministic function applies it.",
  "instruction": { "$file": "instruction.md" },
  "toolsets": ["POD"],
  "visibility": "POD",
  "output_schema": {
    "type": "object",
    "properties": {
      "decision": { "type": "string", "enum": ["auto_send", "human_review"], "description": "auto_send only when ALL auto-send conditions are met; otherwise human_review." },
      "reason":   { "type": "string", "description": "One-line rationale for the decision." }
    },
    "required": ["decision"]
  },
  "permissions": { "grants": [
    { "resource_type": "datastore_table", "resource_name": "tickets",
      "permission_ids": ["datastore.table.read", "datastore.record.read"] },
    { "resource_type": "datastore_table", "resource_name": "customers",
      "permission_ids": ["datastore.table.read", "datastore.record.read"] }
  ] }
}
